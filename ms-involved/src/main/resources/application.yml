spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/db_conexao_respeito
        username: postgresql
        password: 123456
    flyway:
         baselineOnMigrate: true
         locations: classpath:db/migration
         enabled: true
    jpa:
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                show_sql: true
        hibernate:
            ddl-auto: update

server:
    port: 8081
